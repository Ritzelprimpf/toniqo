package de.ritzelprimpf.toniqo.common

import org.junit.Assert.assertTrue
import org.junit.Test

class CommonSmokeTest {

    @Test
    fun `common package is reachable and tests run`() {
        assertTrue(true)
    }
}
