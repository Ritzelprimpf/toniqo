package de.ritzelprimpf.toniqo.keyfinder

import org.junit.Assert.assertTrue
import org.junit.Test

class KeyFinderSmokeTest {

    @Test
    fun `keyfinder package is reachable and tests run`() {
        assertTrue(true)
    }
}
